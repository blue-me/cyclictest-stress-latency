# cyclictest-stress-latency
Use cyclictest and stress to detect latency, with the goal to decrease latency anywhere

The warning: do not use the isolcpus command and the cgroup function at the same time
